"html prototype" contains of initial prototype of markup and jQuery plugin created for spinner component.

"component" folder contains of two projects - component itself and sample application. 

Execute 

	mvn install 

from "component" folder root in order to assemble both component and sample.